mixin todoForm

    h1 Today's Todo
    form(name="add-todo", method="POST", action="/")
        label I will: 
        input(type="text", placeholder="do", name="myTodo", minlength="10", maxlength="50")
        button(type="submit") ADD