# Todo App 

### built with:
    - NodeJS with ExpressJS
    - MongoDB with Mongoose
    - if (Server Side Rendering) {
          return Pug
      } else {
          return Angular
      }
    - Mocha and Chai
    - Docker   
